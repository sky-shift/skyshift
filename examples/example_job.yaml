kind: Job

metadata: 
  name: hello-world7
  labels:
    testing: hello

spec:
  image: gcr.io/sky-burst/skyburst:latest
  resources:
    cpus: 1
    gpus: 0
    memory: 128
  envs:
    MY_ENV_VAR: "hello"
    PATH: "/bin"
    #- name: MY_ENV_VAR2
    #  value: "hello2"
    # post process k8s way into regular dict
  run: |
    #!/bin/bash
    echo $MY_ENV_VAR; sleep 500; echo bye