kind: Service

metadata:
  name: servicedemo

spec:
  type: NodePort
  primary_cluster: auto
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 8080
      node_port: 30012
      target_port: 80
